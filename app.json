{
  "name": "Start on Heroku: Python",
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "python app.py collectstatic --noinput",
        "test": "python app.py test"
      }
    }
  }
}
